# schemas/ — canonical JSON rules (UTF-8 no BOM, sorted keys, canonical separators, newline termination, integer strings, rational {num,den}, schema_version, SHA-256 deps)

Frozen Phase 1 (v0.4 §20; bound set v0.4.6 F1): `proof_attack.schema.json`
(open-world witness payload, sample-validated) + `pair_access_certificate.schema.json`
(mandatory load-bearing fields, sample-validated). Remaining 13 schemas
(parent_import, survivor_binding, theorem_obligation, proof_certificate,
formal_certificate, review_record, locality_witness, preservation_case,
boundary_witness, repayment_witness, credit_use_graph, integrability_witness,
bridge_audit, negative_family, final_result_v0.4) authored at Phase-1 execution;
sample-validation record for the two frozen schemas lives in Path.md R1-019
(formal `WITNESS_SCHEMA_VALIDATION.json` is a Phase-1 freeze artifact).
