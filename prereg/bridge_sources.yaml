# STUB — L0-L4 exact bytes/versions/SHAs + hypothesis checklist. Populated with final L2/L3 bytes BEFORE FOUNDATION_FROZEN; Phase 02 verifies and writes only the separate freeze file. Frozen Phase 1.
