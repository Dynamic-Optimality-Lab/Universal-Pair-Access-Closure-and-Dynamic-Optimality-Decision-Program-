leanprover/lean4:v4.21.0
# STUB — exact toolchain + elan identity + mathlib commit + lake-manifest hash pinned at Phase-1 execution (WorkPlan Phase 1). No theorem-critical Lean execution before then.
